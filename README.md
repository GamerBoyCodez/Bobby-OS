# Bobby-OS
This is an is coded with python so download it and in command prompt type this in. First thing, pip install PyQtWebEngine. Second thing, Pip install PyQt5. Third thing, pip install psutil. Fourth thing, pip install tk.
